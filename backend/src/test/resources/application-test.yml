# Profil używany przez klasy *IntegrationTest. URL/user/hasło Postgresa wpina
# @DynamicPropertySource z Testcontainera, więc nadpisujemy tylko te wartości,
# których application.yml oczekuje ze środowiska (MAIL_PASSWORD).
spring:
  jpa:
    show-sql: false
  mail:
    host: localhost
    port: 1025
    username: test
    password: test

logging:
  level:
    root: WARN
    pl.dudios.shop: INFO
