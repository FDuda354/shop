# Profil używany przez klasy *IntegrationTest. URL/user/hasło Postgresa wpina
# @DynamicPropertySource z Testcontainera, więc nadpisujemy tylko te wartości,
# których application.yml oczekuje ze środowiska (MAIL_PASSWORD, JWT_SECRET).
spring:
  jpa:
    show-sql: false
  mail:
    host: localhost
    port: 1025
    username: test
    password: test

jwt:
  expirationTime: 2592000000
  secret: test-secret-for-integration-tests-only

logging:
  level:
    root: WARN
    pl.dudios.shopmvn: INFO
